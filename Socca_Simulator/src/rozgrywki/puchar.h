#ifndef PUCHAR_H
#define PUCHAR_H

/**
 * @brief Klasa zarezerwowana pod przyszły tryb rozgrywek pucharowych.
 *
 * @note Klasa jest obecnie jedynie zaślepką (pustym szkieletem) -
 * nie zawiera żadnych pól ani logiki i nie jest jeszcze wykorzystywana
 * w programie. Docelowo tryb pucharowy ma korzystać z klasy drabinka
 * do reprezentacji drabinki turniejowej.
 */
class puchar
{
public:
    /**
     * @brief Tworzy pusty obiekt trybu pucharowego.
     */
    puchar();
};

#endif // PUCHAR_H
